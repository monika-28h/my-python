# Function to find the sum of n numbers
def sum_of_n_numbers(numbers):
    total = 0
    for num in numbers:
        total += num
    return total

# Example usage
nums = [10, 20, 30, 40, 50]
result = sum_of_n_numbers(nums)
print("The total sum is:", result)
